'user server';

export const signIn = async (data: any) => {
    try {
        console.log(data);
    } catch (error) {
        console.error(error);
    } finally {
        // yeah bitch...
    }
}

export const signUp = async (data: SignUpParams) => {
    try {
        
    } catch (error) {
        console.error(error);
    } finally {
        // yeah bitch...
    }
}
